import React from 'react';

import { Text } from 'react-native';

import { BookContainer } from './styles';

const Book: React.FC = () => (
  <BookContainer>
    <Text>Book</Text>
  </BookContainer>
);

export default Book;
