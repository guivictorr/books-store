export interface ListPageProps {
  key: string;
  name: string;
  params: {
    subjectTitle: string;
  };
}

export interface SubjectProps {
  title: string;
}

export interface HeaderProps {
  title: string;
}
